<% if flash.any? %>
  <% user_facing_flashes.each do |key, value| %>
    <div class="alert alert-dismissible alert-<%= to_bootstap_class(key) %>">
      <button class="close" data-dismiss="alert">
        <span>&times;</span>
      </button>
      <%= value %>
    </div>
  <% end %>
<% end %>
