<tr>
  <td><%= group_counter + 1 %></td>
  <!-- the group_counter is given by rendering partial with collection, it works like index -->
  <td><%= link_to(group.title, group_path(group)) %></td>
  <td><%= simple_format(group.description, style: 'color: red') %></td>
  <td><%= group.creator.email %></td>
  <td>
    <% if current_user == group.creator %>
      <%= link_to('編輯', edit_group_path(group), class: 'btn btn-info btn-sm') %>
      <%= link_to('刪除', group_path(group), method: :delete, class: 'btn btn-danger btn-sm', data: { confirm: 'Are you sure?' }) %>
    <% end %>
  </td>
</tr>
